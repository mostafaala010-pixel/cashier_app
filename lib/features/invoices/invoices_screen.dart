import 'package:flutter/material.dart'; import '../../core/database/database_helper.dart'; class InvoicesScreen extends StatefulWidget{const InvoicesScreen({super.key});@override State<InvoicesScreen> createState()=>_V();} class _V extends State<InvoicesScreen>{List<Map<String,dynamic>> inv=[];void initState(){super.initState();ld();}Future<void>ld()async{final db=await DatabaseHelper.instance.database;final r=await db.query('invoices',orderBy:'id DESC');setState(()=>inv=r);}Widget build(BuildContext c){return Scaffold(appBar:AppBar(title:const Text('Invoices Pro')),body:inv.isEmpty?const Center(child:Text('No invoices yet')):ListView.builder(itemCount:inv.length,itemBuilder:(_,i){final it=inv[i];return Card(margin:const EdgeInsets.all(8),child:ListTile(leading:Container(padding:const EdgeInsets.all(10),decoration:BoxDecoration(color:Colors.blue.shade50,borderRadius:BorderRadius.circular(10)),child:const Icon(Icons.receipt_long,color:Colors.blue)),title:Text('Invoice #',style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text(''.toString().substring(0,16)),trailing:Text(' YER',style:const TextStyle(fontWeight:FontWeight.bold,fontSize:16)))) ;}));}}
